@forelse($recentTasks as $task)<tr><td><a class="file-link" href="{{ route('admin.tasks.show',$task) }}">{{ $task->task_id }}</a><div style="margin-top:3px;font-weight:700">{{ $task->task_name }}</div></td><td>{{ $task->designer?->name ?? '—' }}</td><td><span class="badge badge-dark">{{ ucwords(str_replace('_',' ',$task->status)) }}</span></td><td>{{ $task->due_at?->format('d M Y · h:i A') }}</td></tr>@empty<tr><td colspan="4" class="empty-state">{{ ($search ?? '') !== '' ? 'No matching tasks found.' : 'No tasks available.' }}</td></tr>@endforelse
